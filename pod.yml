apiversion: v1
metadata:
  name: podbyyml
kind: Pod
spec:
  containers:
    -name: nginx-container
    -image: nginx-latest
    -ports:
      -containerPort: 80

      #ths is to do chenges
      #and check git only
