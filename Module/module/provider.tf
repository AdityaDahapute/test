provider "aws" {
    region = "us-east-2"
    profile = "configs" #masking -. in my local system thee is masking done at word cofigs with accss keys in it this is done to keep senitive data safe//if u dont understand then google masking keys in teraform 
}

