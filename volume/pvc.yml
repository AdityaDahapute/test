apiVersion: v1
metadata:
  name: pv-claim
  labels:
    app: pv-claim
kind: PersistentVolumeClaim
spec:
  storageClassName: standard
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 14Gi
